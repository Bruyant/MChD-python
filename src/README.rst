Upon cloning the directory, use the following commands to install anll the dependencies and be ready to use the software:

cd <uour_storage>/MChD-python/src
conda env create -f environment.yml
conda activate MChD
